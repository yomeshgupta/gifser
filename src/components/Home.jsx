import React, { Fragment } from 'react';

import Header from './Header';
import Content from './Content/index';

const Home = () => {
	return (
		<Fragment>
			<Header />
			<Content />
		</Fragment>
	);
};

export default Home;
